# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

.DS_Store

# offline art tooling (see docs/PIPELINE.md)
tools/.venv/
tools/__pycache__/

# everything tools/ derives from the master (see docs/SCENE.md).
# art/ itself keeps only the four authored inputs; art/build/ regenerates from them.
art/build/

# SHARP's reconstruction. 63MB, and a tool output rather than an authored input - the
# rasters the site actually loads are baked from it into public/art/ and committed there.
# Keep it outside git; see art/README.md.
art/*.ply
